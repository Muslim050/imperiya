Hero banner assets.

Expected file:
- `engelberg.png` — the Engelberg window visual shown on the right side of the
  hero (see `components/sections/Hero.tsx`). A `.jpg` with the same name also
  works — the component falls back to `engelberg.jpg`, then to a dark panel if
  neither is present. Recommended: 16:9, dark/black background so it blends into
  the near-black hero panel.
