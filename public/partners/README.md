# Partner logos

Drop a PNG/SVG/JPG per partner here. The `Partners` section reads the path
from `PARTNERS` in `data/catalog.ts`. If the file is missing the card
falls back to the wordmark.

Current list:
- akfa.png        ← Akfa
- rehau.png       ← REHAU  (missing — drop file to enable)
- benkam.jpg      ← Benkam
- continental.png ← Continental
- kinlong.png     ← Kin Long
- roto.png        ← Roto
- wink.png        ← Wink

Recommended: transparent-background PNG or SVG, max ~150×56 px display
slot (image is `object-contain` so source proportions are preserved).
